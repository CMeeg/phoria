# Phase 5 Close-Out Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Take `feature/examples-parity` from "implemented but unverified" to merge-ready against canary, with the `TODO.md` and phase-5-plan ledgers accurate again.

**Architecture:** The Phase 5 implementation (nine examples, layout-aware tooling, CI bounds, phase renumbering) is already committed on `feature/examples-parity`; its plan checkboxes were never ticked and the Task 13 verification matrix was never run. This plan closes the four remaining loose ends (inspect-config removal, two missing example READMEs, a cosmetic appsettings alignment) and runs the full verification matrix before the canary merge that unblocks the TODO.md "prod testing" item.

**Tech Stack:** pnpm 11 workspaces + Turborepo + Changesets; Vite 8/Rolldown islands per example; Aspire AppHosts; Node.js v24.18.0, .NET SDK 10.0.302.

**Spec:** [`docs/superpowers/specs/2026-08-16-examples-parity-design.md`](../superpowers/specs/2026-08-16-examples-parity-design.md) — the plan argues from the spec, so the spec travels with it; executors read both. Task-by-task implementation detail lives in [`docs/superpowers/plans/2026-08-16-examples-parity.md`](../superpowers/plans/2026-08-16-examples-parity.md) (Tasks 1–12 complete, Task 13 pending this plan).

## Global Constraints

- Commit messages and prose in British English; Biome formatting (tabs, no trailing commas, line width 120).
- Example refs must stay on **published** registry ranges — `pnpm examples:check` fails on `link:`/`file:` phoria refs or a Phoria `ProjectReference`.
- Companion `node_modules` and `.vite-config` output are git-ignored; never force-add them.
- Example scripts run from `WebApp/` (or `apps/WebApp/` for `with-workspace`); Vite resolves config from `process.cwd()`.
- Every example keeps its committed lockfile in sync with its `package.json` (no uncommitted lockfile drift).
- Phases 0–4 complete; this branch must land on **canary**, not `main`. The `@phoria/opentelemetry` package must have been published for standalone giget installs to resolve.
- Port map is collision-free (catalog: getting-started 5373/5273, framework-multiple 5573/5473, framework-react 5173/5073, framework-svelte 5473/5373, framework-vue 5273/5173, with-tailwind 5773/5673, with-styled-components 5873/5773, with-storybook 5973/5873, with-workspace 5673/5573).

## Review Focus

The input classes the parity spec implies but no task's tests exercise, that are most likely to bite a person using the examples:

1. **Missing dev dependency after plugin removal** — any example whose `vite.config.ts` still imports `@meeg/vite-plugin-inspect-config` after its `package.json` dep is removed, or vice versa. Expected: both are removed together in every single-WebApp example and a fresh `pnpm install` resolves cleanly (Task 1 Step 4 grep pin).
2. **A committed example that no longer builds standalone** — the whole point of the examples is giget-fetch → `pnpm install && pnpm build`. Expected: every example builds from a clean fetch of committed registry refs (Task 5 Step 8).
3. **Lockfile drift silently committed** — removing a dependency in 8+ examples must not leave `pnpm-lock.yaml` files describing the old dep set. Expected: `git status` shows only intended lockfile changes and they are committed with the `package.json` change (Task 1 Steps 2–4, Task 5 Step 6 reversibility).
4. **Allow-list selection has no effect or fails silently** — a typo'd `EXAMPLES_E2E` name must fail fast before any stack starts, and a valid subset must run only those examples. Expected: both behaviours observed (Task 5 Steps 3–4).
5. **Storybook build regresses on unpinned Vite** — `with-storybook` must stay on `~8.0.16` until the Rolldown regression is fixed; a later dependency bump must not silently ship the unpinned range. Expected: `build:storybook` succeeds on the pinned range (Task 5 Step 5).

---

## Tasks

### Task 1: Remove `@meeg/vite-plugin-inspect-config` from all nine examples

**Files:**
- Modify: `examples/{framework-multiple,framework-react,framework-svelte,framework-vue,getting-started,with-storybook,with-styled-components,with-tailwind}/WebApp/package.json` (the `"@meeg/vite-plugin-inspect-config": "~0.3.0"` dependency line)
- Modify: same set of `WebApp/vite.config.ts` files (`import { inspectConfig } from "@meeg/vite-plugin-inspect-config"` + the `inspectConfig()` entry in the plugins array)
- Modify: `examples/with-workspace/apps/WebApp/package.json` and `examples/with-workspace/apps/WebApp/vite.config.ts` (alternate layout — same removal, tab indentation)
- Modify: the accompanying `pnpm-lock.yaml` files (regenerated by `pnpm install`)
- Test: repository-wide grep for `inspect-config` in committed example files (excluding `node_modules`), then `pnpm examples:check`

- [ ] **Step 1: Remove the import and plugin call from each `vite.config.ts`**
  Before (each of the nine files):
  ```ts
  import { inspectConfig } from "@meeg/vite-plugin-inspect-config"
  ...
  plugins: [dotnetDevCerts(), phoria(), phoriaReact(), inspectConfig()],
  ```
  After:
  ```ts
  plugins: [dotnetDevCerts(), phoria(), phoriaReact()],
  ```
  (`framework-multiple` keeps its full `phoriaReact(), phoriaSvelte(), phoriaVue()` list; `with-storybook`/`with-tailwind` keep `tailwindcss()`; `with-workspace` keeps the `phoriaReact({ workspacePackages: [...] })` argument.)
- [ ] **Step 2: Remove the dependency from each `package.json`** — delete the `"@meeg/vite-plugin-inspect-config": "~0.3.0",` line (line ~39 in `examples/getting-started/WebApp/package.json`; confirm each file; `with-workspace` uses tab indentation).
- [ ] **Step 3: Regenerate each example's lockfile** — run `pnpm install` in each of the eight `WebApp/` directories and in `examples/with-workspace` (its root workspace), then confirm `git status` shows only `package.json` + `pnpm-lock.yaml` changes.
- [ ] **Step 4: Prove the removal is complete** — `grep -rn "inspect-config\|inspectConfig" examples --include=package.json --include=vite.config.ts --include=pnpm-lock.yaml` (excluding `node_modules`) → zero hits in committed files; then `pnpm examples:check` → all nine examples pass.
- [ ] **Step 5: Commit** — `git commit -m "feat(examples): drop inspect-config plugin from examples"` (staging the nine `package.json`, nine `vite.config.ts`, and their lockfiles).

### Task 2: Add READMEs for `getting-started` and `framework-multiple`

**Files:**
- Create: `examples/getting-started/README.md`
- Create: `examples/framework-multiple/README.md`

Seven of nine examples have READMEs; these two (the pre-existing, non-parity examples) do not. Follow the structure of the other seven: install / dev / preview / stop / e2e commands, feature-specific behaviour, and the `gh:cmeeg/phoria/examples/<name>` giget path.

- [ ] **Step 1: Write `examples/getting-started/README.md`** — document React + basic SSR/hydration/health-check, ports `5373/5273` from the catalog, and the full command list (install, build, dev, preview, test:e2e, stop).
- [ ] **Step 2: Write `examples/framework-multiple/README.md`** — document React + Svelte + Vue islands, query-filtered islands, OpenTelemetry, and ports `5573/5473` (the multi-framework example).
- [ ] **Step 3: Cross-check every command in both READMEs against the example's `package.json` scripts** — each command must resolve; README commands run from `WebApp/` as per the contributor contract.
- [ ] **Step 4: Commit** — `git commit -m "docs(examples): add readmes for pre-existing examples"`.

### Task 3: Align `with-workspace` appsettings with `framework-react`

**Files:**
- Modify: `examples/with-workspace/apps/WebApp/appsettings.json`

`framework-react`'s `appsettings.json` carries three `LogLevel` entries under `System.Net.Http.HttpClient` (`OtlpLogExporter`, `OtlpMetricExporter`, `OtlpTraceExporter` → `Warning`) that `with-workspace` lacks — the last visible "difference with framework-react" in TODO.md. The workspace AppHost already calls `WithOtlpExporter`, so this is OTLP HTTP-client noise suppression for consistency, not new functionality.

- [ ] **Step 1: Add the three entries** to `examples/with-workspace/apps/WebApp/appsettings.json`, matching `framework-react`'s values and the file's tabs indentation.
- [ ] **Step 2: Verify JSON validity and parity** — `node -e "JSON.parse(...)"` succeeds, and the `phoria` section is unchanged apart from `server.port` (5573).
- [ ] **Step 3: Commit** — `git commit -m "chore(examples): align workspace appsettings with react example"`.

### Task 4: Reconcile the ledgers

**Files:**
- Modify: `TODO.md`
- Modify: `docs/superpowers/plans/2026-08-16-examples-parity.md`

The drift that caused the recall problem. Tick the completed work in both ledgers; the plan's Tasks 1–12 are green-verified by Tasks 1–3 and 5 below and are ticked now, Task 13 only after Task 5 records its results.

- [ ] **Step 1: Tick Tasks 1–12 in `docs/superpowers/plans/2026-08-16-examples-parity.md`** — every `- [ ]` under Tasks 1–12 becomes `- [x]`; leave Task 13 unchecked until Task 5 runs.
- [ ] **Step 2: Update TODO.md** — mark done (with the plan reference where one exists):
  - `Implement component path fix plan` (resolved; `docs/plans/2026-08-16-component-path-for-workspace-packages.md`)
  - the example test-checklist items that are now verified (framework-*, getting-started, with-storybook, with-styled-components, with-tailwind, with-workspace runtime/styling items)
  - `Split examples phase into two?` (Phase 5 + Phase 10 in PROJECT.md)
  - `Create a checklist for adding a new example…` (contributor contract in `examples/README.md`)
  - `Add a docs phase…` (Phase 6 in PROJECT.md)
  - the `framework-*`, `with-workspace`, `with-styled-components`, `with-tailwind`, `with-storybook` entries under the second `## Examples` block (shipped via parity)
- [ ] **Step 3: Decide the dev-cert warning entries** — the `Developer certificates may not be fully trusted…` items on with-styled-components/with-tailwind/with-workspace are Aspire/OS cert-trust noise, not Phoria defects; mark them done with a note, or leave them if you judge them environment-specific. `Ruling:` the outcome.
- [ ] **Step 4: Commit** — `git commit -m "chore: reconcile todo and phase 5 plan checkboxes"`.

### Task 5: Run the Task 13 verification matrix

Ordered cheapest → dearest so failures surface early. Results go into `docs/superpowers/plans/2026-08-16-examples-parity.md` Task 13 (tick the steps after the runs) and a commit.

- [x] **Step 1: `pnpm examples:check`** from the repo root → all nine examples pass (committed registry refs; also proves Task 1 clean). — ✅ 9/9.
- [x] **Step 2: Repository checks in CI order** — `pnpm build` → `pnpm lint` → `pnpm check` → `pnpm test` → `pnpm test:browser` → `dotnet test --solution Phoria.sln --configuration Release`; each green before the next. — ✅ all green in order.
- [x] **Step 3: Invalid allow-list selection** — `EXAMPLES_E2E=does-not-exist pnpm examples:e2e` → clean non-zero exit with a clear error before any stack starts. — ✅ exits 1, `Unknown example name(s)`, pre-stack.
- [x] **Step 4: CI allow-list run** — `EXAMPLES_E2E=getting-started,framework-multiple,with-workspace pnpm examples:e2e` → only those three execute and pass. — ✅ ran (the three only); surfaced a ruled finding: `with-workspace` fails `emits modulepreload directives` — pre-existing registry version skew (`0.5.0-beta.0` predates the workspace `__phoriaComponentPath` feature; passes 3/3 under linked HEAD). Closed as a recorded post-merge unblock (0.6.x publish → examples-sync bump → re-run gate), see ledger ruling; CI allow-list itself behaves correctly.
- [x] **Step 5: Storybook build** — `pnpm --dir examples/with-storybook/WebApp build:storybook` → succeeds on the pinned `~8.0.16` Vite range. — ✅ succeeds on pinned `~8.0.16`.
- [x] **Step 6: Link/sync reversibility** — `pnpm examples:link` then `pnpm examples:sync` across all examples → `git status` shows no generated link/sync changes afterward. — ✅ clean (one tab-authored `package.json` re-indented by sync's `writeJson`; restored byte-exact — ledgered tooling note).
- [x] **Step 7: Unrestricted local e2e** — `pnpm examples:e2e` → all nine examples execute sequentially and pass. Longest run; do last. — ⚠️ ran; 8/9 green: with-styled-components/with-tailwind were initially failing on a stale `react-counter` marker assertion and were fixed by asserting the server-rendered counter state (`count is 5`) instead (maintainer-directed test change; marker class not re-added); `with-workspace` remains the ruled version-skew fail (script aborts on first failure — workspace is last alphabetically; other eight verified sequentially).
- [x] **Step 8: Giget standalone fetch** — for each of the nine examples: `pnpm dlx giget gh:cmeeg/phoria/examples/<name> <tmp-dir>` → standalone `pnpm install` → `pnpm build`. Record which examples needed the post-merge beta publish to resolve. — ⛔ feasibility-probed: `examples/` 404s on the default branch (`main`); ALL nine need the post-merge beta publish to resolve (recorded, Task 13 Step 2 + ledger).
- [x] **Step 9: Record results and commit** — fill in Task 13's checkboxes in the parity plan with pass/fail and the commands run; `git commit -m "chore: record phase 5 verification results"`. — ✅ this commit.

### Task 6: Merge sequencing and the unblocked TODO

Executed with the human partner (a merge to a shared branch is outside the executor's authority to perform unprompted).

- [x] **Step 1: Open `feature/examples-parity` → `canary` PR** (canary accepts feature branches; the main-branch source guard does not apply). — ✅ PR #37, squash-merged to `canary` (`092d6ff`).
- [x] **Step 2: After merge, the release workflow publishes `beta` versions and opens the `chore/examples-sync-*` PR** (existing `release.yml` behaviour). — ✅ PR #38 published the betas; PR #39 (`chore(examples): sync to latest phoria packages`) merged 2026-09-24. Published: npm `@phoria/phoria` / `phoria-react` `0.5.0-beta.1`, `phoria-svelte` / `phoria-vue` `0.4.0-beta.1`, `@phoria/opentelemetry` `0.2.0-beta.2`; NuGet `Phoria 0.5.0-beta.1`. The workspace feature shipped in the `0.5.x` stream, not the `0.6.x` this plan predicted — a superseded prediction, recorded in the ledger.
- [x] **Step 3: Unblock "prod testing of all examples"** — run `prod`/preview and standalone builds against the published betas (the TODO item's "wait until next merge into canary" is exactly this step). — ✅ DONE 2026-09-25, no code change required. **Production docker, 9/9 green**: sequential `docker compose up --build -d` per example against published `0.5.0-beta.1`, each reaching `/health` body `Healthy` and then passing its own e2e suite against the container (framework-multiple 5, framework-react 4, framework-svelte 4, framework-vue 4, getting-started 3, with-storybook 4, with-styled-components 5, with-tailwind 5, with-workspace 3). **Standalone giget, 9/9 green**: fetch → `pnpm install --frozen-lockfile` → `pnpm build` with no source repo and no root workspace. Both TODO items ticked.
- [x] **Step 4: Post-merge MEMORY entry** — record the close-out and that `phoria-examples` is archived only after parity reaches `main` (per the parity design). — ✅ `docs/MEMORY.md` entry dated 2026-09-25, including both retractions above and the two verification traps. `phoria-examples` remains **unarchived** on purpose: `examples/` is not on `main` yet (PR #36 open), and the unqualified giget paths in the example READMEs keep 404ing until it merges.

#### Correction: the `phoria.server.host` "core fix candidate" was a misattribution

The close-out review left a note that a `phoria.server.host` default change was a future core fix candidate, on the strength of a with-workspace monitor failure attributed to `localhost` resolving to `::1` only. **That attribution is wrong**, and the note should not be actioned. Measured in the real `mcr.microsoft.com/dotnet/aspnet:10.0` image against published `0.5.0-beta.1`, with no `host` key set anywhere: the node child binds `[::1]:5273` **only** (there is no IPv4 listener), the monitor's connection is ESTABLISHED **on that IPv6 socket**, and `GET /health` returns body `Healthy`. .NET resolves `localhost` to `::1` and reaches node successfully, so the `localhost` default works. An A/B on with-workspace confirms it: no host key and `"127.0.0.1"` pinned are **both** `Healthy`, so the original failure does not reproduce.

The review's recorded probe URL `http://0.0.0.0:5573` cannot come from any committed config: `GetServerUrl()` uses `Server.Host` verbatim, there is no `0.0.0.0` in `packages/Phoria` or in any example JSON, and git history shows with-workspace's `appsettings.Production.json` never had a `host` key before `fdca4e2` *added* `"127.0.0.1"`. The `0.0.0.0` was leftover local state from that debugging session, and pinning the host papered over it.

Two further lessons for anyone re-testing this. The startup window is real: the monitor reports `Unhealthy` until the node child binds, so a probe fired in the first second or two sees a **genuine 503** that resolves moments later — allow for it before concluding a container is broken, and require two consecutive healthy readings rather than trusting the first. The status code itself is correct: all nine examples call `app.MapHealthChecks("/health")` with no options, so the ASP.NET Core default mapping applies and `Unhealthy` returns **503** (verified by forcing the node child to fail — 503 with body `Unhealthy`), while `Degraded` returns 200 so a `Degrade` deployment is not killed by its own probe. That mapping was the one behaviour no committed test could see, because every example e2e suite only ever probes a healthy container; it is now pinned by `packages/Phoria.Tests/Diagnostics/HealthChecks/PhoriaServerHealthCheckEndpointTests.cs`, which starts a real host and asserts 503 under `Fail` and 200 under `Degrade`. And the example Dockerfiles resolve phoria from the **registry**, so a `docker compose` run tests the published betas and can never validate a working-tree change; that is a reason to be careful about what a prod run proves, not a reason to skip it.

## Risks and Mitigations

- **Low — inspect-config removal:** nothing in `scripts/` or `.github/` references the plugin (verified); it was the Vite 6→8 diff-baseline tool and is vestigial in examples. Mitigation: Task 1 Step 4 grep + `examples:check`.
- **Medium — giget fetches resolve latest published ranges:** the release-sync PR keeps examples current, so standalone fetches are most meaningful against post-sync state. Mitigation: Task 5 Step 8 records which examples need the beta publish rather than failing the phase on it.
- **Time — the unrestricted nine-example e2e is the longest run in the matrix:** the allow-list run (Task 5 Step 4) is the CI gate; the unrestricted run is due diligence. Mitigation: order the matrix cheap-first, and if e2e infrastructure is unavailable in the execution environment, record it explicitly in Task 5 Step 9 rather than silently skipping.
- **Low — plan-checkbox sweep is a mechanical edit with a review signal:** a ticked task whose verification is missing is the failure mode the sweep exists to prevent. Mitigation: Task 4 ticks only after Tasks 1–3 and 5 verify, never before.

## Commit Strategy

Each task lands one commit (Task 1 is the one multi-file commit: 18 example files + 9 lockfiles). Commit messages follow the repo convention seen on this branch (`feat(examples):`, `docs(examples):`, `chore(examples):`, `chore:`). The Task 5 results commit is the last code-side commit on the branch before the canary PR; the ledger sweep (Task 4) is deliberately sequenced before Task 5 so the verification results are the only outstanding record.